# Images

Shared visual assets used across pages.

## Naming notes
- Keep file names descriptive and lowercase when possible.
- Prefer hyphen-separated names (example: `positional-play-diagram.png`).
- Use optimized image sizes to reduce page load time.
